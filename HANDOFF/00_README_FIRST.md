# 🔴 새 Claude에게 — 먼저 이걸 읽어주세요

> 사용자: 하현성 (아주대 4학년, 딥러닝응용 기말 프로젝트, 3조)
> 인수인계 시점: 2026-06-08
> 마감: 곧 (최종 발표 임박)
> **이전 Claude의 사용량이 소진되어 인계됨. 이전 대화 컨텍스트 없이 시작합니다.**

---

## 📌 30초 요약

- **과제**: 멀티모달 딥러닝(센서+카메라) 기반 제조 공정 이상 탐지 — 발표·보고서 제출
- **현재 상태**: PPT 1차본 완성, **추가실험 ⑤(균형 통제 v2) 결과가 막 나옴 → PPT 반영 필요**
- **사용자 성향**: 정직 선호. 모호한 비유·내부 디버깅 용어 싫어함. 청중이 이해할 수 있게 풀어 설명 요구.
- **금기**:
  - "사과 대 사과" 같은 비유 ❌
  - "불공정 비교", "숨기는 게 아니라" 등 내부 디버깅 톤 ❌
  - 한 줄짜리 발표 스크립트 ❌ (10분 분량은 슬라이드당 40~75초 풀어쓰기)

---

## 🎯 즉시 할 일 — 우선순위 1번

**`v13_full_results.txt`의 추가실험 ⑤ 결과를 PPT에 반영하기.**

### 추가실험 ⑤ 결과 (이미 검증됨)
```
[균형 통제 v2] 센서 h16/2ep + 이미지 약화 3ep · FAIR 전체 9,521 시점
Sensor (very weak)  F1 : 0.8757   ← 사용자가 원했던 ~0.8 수준 달성!
Image  (weak)       F1 : 0.1586   (결측 77% 포함, 낮음 정상)
Decision-Fusion     F1 : 0.8766   (w=0.40)
→ FAIR 전체 멀티모달 이득 O (0.8766 > 0.8757)

[보조] 같은 모델, 이미지 존재 구간 2,161 시점:
  Sensor F1            : 0.8984
  Image  F1            : 0.2643
  Decision-Fusion F1   : 0.9004
  두 단독 차이: 0.6341
  → 균형 구간 멀티모달 이득 O
```

→ 이게 사용자가 가장 원했던 "센서·이미지 둘 다 약화시켜 ~0.8 수준에서 비교" 결과.
→ **`make_result_figs.py`의 `res_story.png` 그래프를 이 신규 수치로 갱신**하고 PPT 빌드 한 번 더 돌리면 됨.

### 작업 순서
1. `make_result_figs.py` 열기 → `res_story` 그래프의 `sensor_v`, `image_v`, `fusion_v` 값 갱신
2. 시나리오 라벨도 적절히 (예: "Both weakened (h16,2ep)" 또는 "Balanced ~0.8")
3. `python make_result_figs.py` 실행
4. `python build_ppt.py` 실행 → PPT 갱신
5. PowerPoint COM(`scripts/qa_render.py` 만들어 쓰거나) 또는 사용자가 직접 열어서 확인

### ⚠️ 신규 수치를 발표 노트에도 반영
- 슬라이드 7 (추가 검증) 스크립트 갱신
- 결론 슬라이드 수치도 일관성 확인

---

## 📂 폴더 안 파일 가이드 (이 HANDOFF 폴더만 있으면 됩니다)

| 파일 | 용도 |
|------|------|
| **`00_README_FIRST.md`** | 이 파일 (가장 먼저 읽기) |
| **`01_PROJECT_CONTEXT.md`** | 프로젝트 전체 맥락 (목적·데이터·방법론) |
| **`02_USER_PREFERENCES.md`** | 사용자가 좋아하는/싫어하는 톤·표현 |
| **`03_RESULTS_ALL.md`** | 모든 실험 결과 정리 (Version별·실험별) |
| **`04_PPT_STATUS.md`** | 현재 PPT 상태 + 갱신 필요한 부분 |
| **`v13_full_results.txt`** | Version 13 노트북 실행 출력 전체 (raw) |
| **`notebook_LATEST.ipynb`** | 노트북 코드 최신본 (38셀, 추가실험 ⑤까지) |
| **`notebook_V13_with_outputs.ipynb`** | 노트북 + 실행 결과 다 포함 (Kaggle 다운로드본) |
| **`PPT_current_v2.pptx`** | 현재 PPT (12슬라이드, ~9분 35초) |
| **`발표대본_v2.md`** | 발표 대본 (슬라이드별 스크립트) |
| **`DEBUGGING_LOG.md`** | 프로젝트 진행 중 모든 시행착오 기록 |
| **`REPORT_초안_하현성.docx`** | 보고서 초안 (구버전 결과 반영, 신규 결과로 업데이트 필요) |
| **`build_ppt.py`** | PPT 빌드 스크립트 (직접 수정 가능) |
| **`make_result_figs.py`** | 결과 그래프 생성 (균형통제 시나리오 수치 수정 위치) |
| **`make_dataset_figs.py`** | 데이터셋 그래프 생성 |
| **`ppt_assets/`** | 생성된 그래프 이미지들 |
| **`TEMPLATE_중간발표.pptx`** | 디자인 템플릿 (헤더·표지 유지 필수) |
| **`reference_NSF-MAP.pdf`** | 참고 논문 (IJCAI 2025) |

---

## 🚦 즉시 답할 수 있어야 할 핵심 사실

### 데이터 구조 (자주 헷갈리는 부분)
- **레코드 166,001개** = CSV 한 줄(타임스텝). 1.95Hz 측정
- **사이클 285개** = 제품 285개 만듦 (한 사이클 = 한 제품 조립 완료, 평균 582 타임스텝)
- **CycleState 1~21** = 사이클 내 단계 번호
- **이미지는 CycleState 4·9만 사용** (논문 NSF-MAP 근거 — `Filter cycle state 4 & 9 → Crop → Object Detection`)
- **라벨은 매 타임스텝마다 따로 붙어 있음** (제품 단위 양/불 아님 → "지금 이 순간 어떤 상태인가" 분류 과제)

### 평가 시점 집합 (사용자가 가장 헷갈려한 부분)
- **FINAL SUMMARY**: 센서=전체 사이클(30,132), 이미지=cycle 4·9의 2,346, Fusion=cycle 4·9의 9,521 → 서로 다른 셋 (비교 어려움, 발표에서 안 씀)
- **FAIR COMPARISON**: 모든 모델 동일 cycle 4·9 셋 (9,521 시점)에서 평가
- **이미지 존재 구간**: 9,521 중 이미지가 실제 매칭된 2,161 시점만

### 핵심 결과 (Version 12 + 13 종합)
| 평가 | Sensor | Image | best Fusion |
|------|--------|-------|-------------|
| FAIR 전체 (9,521) | 0.8710 | 0.5929 | 0.8714 (미미) |
| 이미지 존재 (2,161) — 정상 학습 | 0.9739 | 0.9285 | **0.9814** ★ |
| 이미지 약화 (2,161) | 0.9739 | 0.7751 | 0.9779 |
| **균형 통제 v2 — FAIR 전체** | **0.8757** | 0.1586 | **0.8766** |
| **균형 통제 v2 — 존재 구간** | **0.8984** | 0.2643 | **0.9004** |

---

## 🗣️ 사용자가 가장 신경 쓰는 것

1. **분량**: 발표 정확히 9~10분 (10분 넘으면 안 됨). 슬라이드당 40~75초 풀어쓴 스크립트가 노트에 있어야 함.
2. **정직성**: 결과를 부풀리지 않음. 단, 핵심 발견은 명확히 강조.
3. **이해 가능성**: 청중이 멀티모달 모른다 가정. 용어·맥락 다 설명.
4. **디자인**: 중간발표 PPT 템플릿(`TEMPLATE_중간발표.pptx`)의 표지·헤더 디자인 유지.

---

## ❌ 사용자가 명시적으로 화낸 부분 (반복 금지)

1. "사과 대 사과 비교" 같은 비유 — "갑자기 사과는 왜 나옴?"
2. "불공정/공정 비교" 같은 내부 디버깅 용어를 PPT에 그대로 씀 — "보고서엔 들어가도 PPT엔 헷갈리게 만들 필요 없음"
3. "숨기는 게 아니라 솔직하게" 같은 우리끼리 대화 톤 — "갑자기 뭘 숨기는데?"
4. 슬라이드당 한 줄짜리 스크립트로 "10분 분량"이라 한 것
5. 결과 수치 평가 조건을 명시 안 함 — "0.9였다가 0.4면 왜 다른지 설명도 없음"
6. 폴링이 인증 거부로 처음부터 작동 안 했는데 안 알린 것 (Kaggle API: hyunsungha 계정 키만 있음, kimjoohoen 계정은 접근 불가)

---

## 🔄 다음 작업 흐름 (추천)

1. **이 폴더 안 모든 .md 읽기** (`00`→`04` 순서)
2. **`v13_full_results.txt`** 읽어서 새 결과 파악
3. **`make_result_figs.py`에서 `res_story` 부분 수정** → 그래프 재생성
4. **`build_ppt.py` 실행** → PPT 갱신 (균형통제 슬라이드 신규 수치 반영)
5. **PPT 시각 QA** (`scripts/qa_render.py` 만들거나 사용자가 확인)
6. **발표 대본 마지막 갱신** — 균형 통제 부분 스크립트 신규 수치로
7. **보고서(`REPORT_초안_하현성.docx`)는 그 다음**

---

## 💡 도움될 만한 명령어

```bash
# 결과 그래프 재생성
cd "프로젝트 폴더"
python make_result_figs.py

# PPT 빌드
python build_ppt.py

# 노트북 셀 내용 빠르게 보기
python -c "import json; nb=json.load(open('notebook_LATEST.ipynb',encoding='utf-8')); print(len(nb['cells']))"
```

---

**이 README와 다른 04개 파일을 다 읽으면 이전 Claude가 어디까지 했는지 정확히 파악할 수 있습니다.**
**모르겠는 부분은 사용자에게 묻지 말고, 먼저 파일을 뒤져보세요.**
